const fs=require('fs'),path=require('path');
const root=__dirname; const js=fs.readFileSync(path.join(root,'assets/js/agent-workspace.js'),'utf8');
const checks=[
['version',value=>{const p=String(value).split('.').map(Number);return p[0]>3||(p[0]===3&&p[1]>=15);}],['reinitialize teardown','__CoverageFitAgentWorkspaceTeardown'],['cleanup registry','lifecycleCleanups'],['listener helper','function listen('],['timer cleanup','function clearWorkspaceTimers()'],['pagehide teardown',"listen(window, 'pagehide'"],['data unsubscribe','unsubscribeWorkspaceData'],['lifecycle diagnostics','CoverageFitAgentWorkspaceLifecycle'],['disposed checklist guard','if (workspaceDisposed) return;'],['named sidebar handler','function handleSidebarToggle()'],['no interval',!js.includes('setInterval(')],['resize managed',"listen(window, 'resize', syncChecklistSidebarForViewport)"]];
let passed=0, failures=[]; for(const [name,needle] of checks){let ok=typeof needle==='boolean'?needle:(name==='version'?needle(fs.readFileSync(path.join(root,'VERSION'),'utf8').trim()):js.includes(needle)); if(ok)passed++; else failures.push(name);} console.log(JSON.stringify({sprint:'WR-1B.7',passed:!failures.length,checks:checks.length,failures},null,2)); if(failures.length)process.exit(1);
