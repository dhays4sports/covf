# FLOW-1.3 — Semantic Entry Context

Status: Implemented

CoverageFit now receives and stores review reason, occupation segment, and housing context as separate values. Existing legacy handoffs using `segment` continue to populate review context for backwards compatibility.
